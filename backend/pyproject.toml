[project]
name = "durianflow-backend"
version = "0.1.0"
description = "Self-hosted faster-whisper backend for Durianflow dictation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
